#include<stdio.h>

int a[6] = {1,9,45,2,3,8};
int b = 0;

int main() 
{
	for (int i = 0; i < 6; i++)
	{
		for (int p = i+1; p < 6; p++)
		{
			if (a[i] > a[p])
			{
				b = a[i];
				a[i] = a[p];
				a[p] = b;

			}

		}
		printf("%d", a[i]);
	}
	return 0;
}

