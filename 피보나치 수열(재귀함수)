#include<stdio.h>
int a = 0; 
int plus(int a);

int main() 
{
	for(a=0; a<10; a++)
	{
		int plus(a);
		printf("%d", plus(a));
		

	}
	return 0;
}

int plus(int a)
{
	if (a == 0) return 0;
	else if (a == 1) return 1;
	else return plus(a - 1) + plus(a - 2);
}
